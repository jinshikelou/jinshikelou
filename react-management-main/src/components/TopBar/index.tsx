import styles from "../TopBar/TopBar.module.scss";

function TopBar() {
    return (
        <div className={styles.header}>
            This is TopBar component
        </div>
    )
}

export default TopBar